---
title: "Color Thumb"
description: A draggable indicator used in color selection tools, letting users fine tune a chosen color with precise adjustments for accurate color customization.
order: 8
published: true
status: primitive
---

## Basic
When you install a color that requires dragging, you'll need a color thumb. Naturally, you wouldn’t want to install it manually. So, for each color that requires this component, it will be installed automatically.
<How toUse="colors/color-area/color-area-channel-demo" />

## Installation

Install the component via the CLI in one command.
<InstallCommand command="npx shadcn@latest add @intentui/color-thumb"/>

## Manual installation

Use this approach if you prefer to install and wire up the component yourself instead of using the CLI.
<InstallCommand command="npm i react-aria-components"/>

<SourceCode toShow='color-thumb'/>
