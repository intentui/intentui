---
title: "Keyboard"
description: Keyboard and focus support ensures smooth navigation with keys, essential for users without a mouse and power users for quick access. It enhances accessibility.
order: 5
references: ["https://react-aria.adobe.com/interactions.html#keyboard-and-focus-behavior"]
published: true
status: primitive
---

## Installation

Install the component via the CLI in one command.
<InstallCommand command="npx shadcn@latest add @intentui/keyboard"/>

## Manual installation

Use this approach if you prefer to install and wire up the component yourself instead of using the CLI.
<InstallCommand command="npm i react-aria-components tailwind-merge"/>
<SourceCode toShow='keyboard' />

## API

### Keyboard

The `Keyboard` component renders keyboard shortcut text.

| Prop | Type | Default |
| --- | --- | --- |
| `className` | `string` | - |
