---
title: "Tag Field"
description: An input component that allows users to enter, display, and manage multiple tags or keywords dynamically. It supports adding, removing, and editing tags.
order: 9
published: true
references: ["https://react-aria.adobe.com/TagGroup#api"]
---

<How toUse="forms/tag-field/tag-field-example" />

## Installation

Install the component via the CLI in one command.

To install, run the following command:

<PackageCommand command="npx shadcn@latest add @intentui/tag-field"/>

## Composed components

This tag field integrates with components like:

<Composed components={['field', 'tag-group']} />

## Manual installation

Use this approach if you prefer to install and wire up the component yourself instead of using the CLI.

For manual installation, use:

<PackageCommand command="npm i react-aria-components @heroicons/react tailwind-merge"/>

<SourceCode toShow='tag-field' />

## Anatomy
Import the components and use them as shown below, adapting the structure to fit each component.
```
import { Label } from "@/components/ui/field"
import { Input } from "@/components/ui/input"
import { TagField } from "@/components/ui/tag-field"
```

```
<TagField>
  <Label/>
  <Input/>
</TagField>
```

## Component API

### TagField

<ApiParameters>
  <ApiDescription>
    **TagField** extends the React Aria Components [**`<TextField>`**](https://react-aria.adobe.com/TextField#api) component.
  </ApiDescription>
  <ApiParameter name="className" type='string' />
  <ApiParameter name="defaultValue" type='string[]' defaultValue='[]' />
  <ApiParameter name="inputValue" type='string' />
  <ApiParameter name="isRequired" type='boolean' />
  <ApiParameter name="name" type='string' defaultValue='tags' />
  <ApiParameter name="onChange" type='(next: Selection) => void' />
  <ApiParameter name="onInputValueChange" type='(v: string) => void' />
  <ApiParameter name="requiredMessage" type='string' />
  <ApiParameter name="splitPattern" type='RegExp' defaultValue='/[,;]/' />
  <ApiParameter name="value" type='Selection' />
  <ApiReference>
    See the [React Aria](https://react-aria.adobe.com/TagGroup#api) for the full API reference.
  </ApiReference>
</ApiParameters>
