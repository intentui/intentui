---
title: "Snippet"
description: A tabbed code snippet UI with copy to clipboard support, built on accessible React Aria snippet, styled with Tailwind, and designed for clean, compact command switching.
order: 3
published: true
references: ["https://react-aria.adobe.com/Tabs#api"]
---

<How toUse="navigation/snippet/snippet-example" />

## Installation

Install the component via the CLI in one command.
<PackageCommand command="npx shadcn@latest add @intentui/snippet"/>


## Manual installation

Use this approach if you prefer to install and wire up the component yourself instead of using the CLI.
<PackageCommand command="npm i react-aria-components"/>

<SourceCode toShow='snippet' />

## Anatomy
```
import {
  Snippet,
  SnippetTab,
  SnippetTabPanel,
  SnippetTabPanels,
  SnippetTabsList,
} from "@/components/ui/snippet"
```

```
const commands = [
  { id: "npm", label: "NPM", command: "npm install react-aria-components" },
  { id: "pnpm", label: "PNPM", command: "pnpm add react-aria-components" },
  { id: "yarn", label: "Yarn", command: "yarn add react-aria-components" },
  { id: "bun", label: "Bun", command: "bun add react-aria-components" },
]

<Snippet>
  <SnippetTabsList items={commands}>
    {(cmd) => <SnippetTab key={cmd.id}>{cmd.label}</SnippetTab>}
  </SnippetTabsList>
  <SnippetTabPanels items={commands}>
    {(cmd) => <SnippetTabPanel id={cmd.id}>{cmd.command}</SnippetTabPanel>}
  </SnippetTabPanels>
</Snippet>
```

## Component API

### Snippet

<ApiParameters>
  <ApiDescription>
    **Snippet** extends the React Aria Components [**`<Tabs>`**](https://react-aria.adobe.com/Tabs#api) component.
  </ApiDescription>
  <ApiParameter name="className" type='string' />
</ApiParameters>

### SnippetTab

<ApiParameters>
  <ApiDescription>
    **SnippetTab** extends the Intent UI [**`<Tab>`**](/docs/components/navigation/tabs#tab) component.
  </ApiDescription>
  <ApiParameter name="className" type='string' />
</ApiParameters>

### SnippetTabPanel

<ApiParameters>
  <ApiDescription>
    **SnippetTabPanel** extends the React Aria Components [**`<TabPanel>`**](https://react-aria.adobe.com/Tabs#api) component.
  </ApiDescription>
  <ApiParameter name="className" type='string' />
</ApiParameters>

### SnippetTabPanels

<ApiParameters>
  <ApiDescription>
    **SnippetTabPanels** extends the Intent UI [**`<TabPanels>`**](/docs/components/navigation/tabs#tabpanels) component.
  </ApiDescription>
  <ApiParameter name="className" type='string' />
</ApiParameters>

### SnippetTabsList

<ApiParameters>
  <ApiDescription>
    **SnippetTabsList** extends the Intent UI [**`<TabList>`**](/docs/components/navigation/tabs#tablist) component.
  </ApiDescription>
  <ApiParameter name="className" type='string' />
  <ApiReference>
    See the [React Aria](https://react-aria.adobe.com/Tabs#api) for the full API reference.
  </ApiReference>
</ApiParameters>
