---
title: "Tabs"
description: A component that organizes content into separate panels, allowing users to switch between sections without reloading the page, enhancing navigation efficiency.
order: 3
published: true
references: ["https://react-aria.adobe.com/Tabs#api"]
---

<How toUse="navigation/tabs/tabs-example" />

## Installation

Install the component via the CLI in one command.
<PackageCommand command="npx shadcn@latest add @intentui/tabs"/>


## Manual installation

Use this approach if you prefer to install and wire up the component yourself instead of using the CLI.
<PackageCommand command="npm i react-aria-components tailwind-merge"/>

<SourceCode toShow='tabs' />

## Anatomy
```
import { Tab, TabList, TabPanel, Tabs } from "@/components/ui/tabs"
```

```
<Tabs aria-label="Recipe App">
  <TabList>
    <Tab id="i">Ingredients</Tab>
    <Tab id="m">Meal Plans</Tab>
  </TabList>
  <TabPanel id="i">
    Check the list of ingredients needed for your chosen recipes.
  </TabPanel>
  <TabPanel id="m">
    Discover curated meal plans to simplify your weekly cooking.
  </TabPanel>
</Tabs>
```

## Examples

### With icons
If you want to include icons in your tabs, it's simple to do. This tab setup handles icons smoothly, maintaining a clean design.
<How toUse="navigation/tabs/tabs-icons-example" />

### Orientation
You can set the orientation of the tabs to either `horizontal` or `vertical`.
<How toUse="navigation/tabs/tabs-orientation-example" />

### Links
Tabs can also be used as navigational links. Add the `href` prop to a `Tab` to render it as a link and navigate to the provided URL when selected.

When using a framework-specific link component, integrate it through your client-side routing setup. Learn more in [Client Side Routing](/docs/getting-started/client-side-routing).

Adding `href` also applies the pointer cursor automatically, making the tab behave like an interactive navigation item.

<How toUse="navigation/tabs/tabs-link-example" />

### Disabled
You can disable all tabs by setting the `isDisabled` prop to `true`.
<How toUse="navigation/tabs/tabs-disabled-example" />

You can also disable individual tabs by setting the `isDisabled` prop for each one, like this:
```tsx
<Tab isDisabled id="c">Contact</Tab>
<Tab isDisabled id="a">About Us</Tab>
```

## Component API

### Tab

<ApiParameters>
  <ApiDescription>
    **Tab** extends the React Aria Components [**`<Tab>`**](https://react-aria.adobe.com/Tabs#api) component.
  </ApiDescription>
  <ApiParameter name="className" type='string' />
</ApiParameters>

### TabList

<ApiParameters>
  <ApiDescription>
    **TabList** extends the React Aria Components [**`<TabList>`**](https://react-aria.adobe.com/Tabs#api) component.
  </ApiDescription>
  <ApiParameter name="className" type='string' />
</ApiParameters>

### TabPanel

<ApiParameters>
  <ApiDescription>
    **TabPanel** extends the React Aria Components [**`<TabPanel>`**](https://react-aria.adobe.com/Tabs#api) component.
  </ApiDescription>
  <ApiParameter name="className" type='string' />
</ApiParameters>

### TabPanels

<ApiParameters>
  <ApiDescription>
    **TabPanels** extends the React Aria Components [**`<TabPanels>`**](https://react-aria.adobe.com/Tabs#api) component.
  </ApiDescription>
  <ApiParameter name="className" type='string' />
</ApiParameters>

### Tabs

<ApiParameters>
  <ApiDescription>
    **Tabs** extends the React Aria Components [**`<Tabs>`**](https://react-aria.adobe.com/Tabs#api) component.
  </ApiDescription>
  <ApiParameter name="className" type='string' />
</ApiParameters>

### TabScrollArea

<ApiParameters>
  <ApiDescription>
    **TabScrollArea** extends the native **`<div>`** element.
  </ApiDescription>
  <ApiParameter name="className" type='string' />
  <ApiReference>
    See the [React Aria](https://react-aria.adobe.com/Tabs#api) for the full API reference.
  </ApiReference>
</ApiParameters>
