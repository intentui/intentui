---
title: "Dialog"
description: A small, interactive UI element that overlays content to display important information or actions. It appears over the main interface for user interaction.
order: 2
published: true
status: primitive
references: ["https://react-aria.adobe.com/Dialog#api"]
---

## Basic
There’s not much to see here, but you can check out the [Modal](/docs/components/overlays/modal) and [Color Picker](/docs/components/colors/color-picker) documentation for more details.

<Note>
  When you use related components like `modal` or `sheet`, the dialog component is automatically included, so there's no need for a separate installation.
</Note>

## Installation

Install the component via the CLI in one command.
<InstallCommand command="npx shadcn@latest add @intentui/dialog"/>

## Composed components
<Composed components={['button', 'heading']}/>

## Manual installation

Use this approach if you prefer to install and wire up the component yourself instead of using the CLI.
<InstallCommand command="npm i react-aria-components"/>
<SourceCode toShow='dialog' />
