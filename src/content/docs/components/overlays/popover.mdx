---
title: Popover
description: A small, floating UI element that appears near a trigger to show contextual information or actions. It enhances user experience with non-intrusive overlays.
order: 4
published: true
references: ["https://react-aria.adobe.com/Popover#api"]
---

<How toUse="overlays/popover/popover-example" />

## Installation

Install the component via the CLI in one command.
<PackageCommand command="npx shadcn@latest add @intentui/popover"/>

## Composed components
<Composed components={['button', 'dialog']}/>

## Manual installation

Use this approach if you prefer to install and wire up the component yourself instead of using the CLI.

<PackageCommand command="npm i react-aria-components"/>

<SourceCode toShow='popover'/>

## Anatomy
```
import {
  Popover,
  PopoverBody,
  PopoverClose,
  PopoverContent,
  PopoverDescription,
  PopoverFooter,
  PopoverHeader,
  PopoverTitle,
  PopoverTrigger,
} from "@/components/ui/popover"
import { Dialog } from "@/components/ui/dialog"
```

```
<Popover>
  <PopoverTrigger>Open Popover</PopoverTrigger>
  <PopoverContent>
    <Dialog>
      <PopoverHeader>
        <PopoverTitle>Popover Title</PopoverTitle>
        <PopoverDescription>Popover Description</PopoverDescription>
      </PopoverHeader>
      <PopoverBody>Popover Body</PopoverBody>
      <PopoverFooter>
        <PopoverClose>Close</PopoverClose>
        <PopoverClose>Confirm</PopoverClose>
      </PopoverFooter>
    </Dialog>
  </PopoverContent>
</Popover>
```

## Examples

### Controlled
You can control the popover programmatically.
<How toUse="overlays/popover/popover-controlled-example" />

### Overlay arrow
Popovers come with an arrow that points to the trigger element, but it’s hidden by default. To display it, pass the `arrow` prop.
<How toUse="overlays/popover/popover-arrow-example" />

### Placement
You can change the placement of the popover by passing the `placement` prop.
<How toUse="overlays/popover/popover-placement-example" />

The list above shows the primary directions, but you can also use specific placements like `bottom left` or `top right`. Here are all the available placement options:

| Primary Direction | Placement                                                             |
|-------------------|-----------------------------------------------------------------------|
| `bottom`          | `bottom`, `bottom left`, `bottom right`, `bottom start`, `bottom end` |
| `top`             | `top`, `top left`, `top right`, `top start`, `top end`                |
| `left`            | `left`, `left top`, `left bottom`                                     |
| `start`           | `start`, `start top`, `start bottom`                                  |
| `right`           | `right`, `right top`, `right bottom`                                  |
| `end`             | `end`, `end top`, `end bottom`                                        |


### Custom close
You can easily close the popover by clicking outside of it, or add a custom close button inside using Popover.Close. Just make sure everything is wrapped inside a `Dialog`.
<How toUse="overlays/popover/popover-custom-close" />

### Popover trigger
All the examples above use `<Button/>` as the trigger, but what if you wanna switch it up, like using `<Avatar/>` instead? No sweat, just roll with PopoverTrigger like this:
<How toUse="overlays/popover/popover-trigger-example" />

## Component API

### Popover

<ApiParameters>
  <ApiDescription>
    **Popover** extends the React Aria Components [**`<Popover>`**](https://react-aria.adobe.com/Popover#api) component.
  </ApiDescription>
</ApiParameters>

### PopoverTrigger

<ApiParameters>
  <ApiDescription>
    **PopoverTrigger** extends the Intent UI [**`<DialogTrigger>`**](/docs/components/overlays/dialog#dialogtrigger) component.
  </ApiDescription>
  <ApiParameter name="className" type='string' />
</ApiParameters>

### PopoverContent

<ApiParameters>
  <ApiDescription>
    **PopoverContent** extends the React Aria Components [**`<Popover>`**](https://react-aria.adobe.com/Popover#api) component.
  </ApiDescription>
  <ApiParameter name="arrow" type='boolean' defaultValue='false' />
  <ApiParameter name="className" type='string' />
</ApiParameters>

### PopoverHeader

<ApiParameters>
  <ApiDescription>
    **PopoverHeader** extends the Intent UI [**`<DialogHeader>`**](/docs/components/overlays/dialog#dialogheader) component.
  </ApiDescription>
  <ApiParameter name="className" type='string' />
  <ApiParameter name="description" type='string' />
  <ApiParameter name="title" type='string' />
</ApiParameters>

### PopoverTitle

<ApiParameters>
  <ApiDescription>
    **PopoverTitle** extends the Intent UI [**`<DialogTitle>`**](/docs/components/overlays/dialog#dialogtitle) component.
  </ApiDescription>
  <ApiParameter name="className" type='string' />
</ApiParameters>

### PopoverDescription

<ApiParameters>
  <ApiDescription>
    **PopoverDescription** extends the Intent UI [**`<DialogDescription>`**](/docs/components/overlays/dialog#dialogdescription) component.
  </ApiDescription>
  <ApiParameter name="className" type='string' />
</ApiParameters>

### PopoverBody

<ApiParameters>
  <ApiDescription>
    **PopoverBody** extends the Intent UI [**`<DialogBody>`**](/docs/components/overlays/dialog#dialogbody) component.
  </ApiDescription>
  <ApiParameter name="className" type='string' />
</ApiParameters>

### PopoverFooter

<ApiParameters>
  <ApiDescription>
    **PopoverFooter** extends the Intent UI [**`<DialogFooter>`**](/docs/components/overlays/dialog#dialogfooter) component.
  </ApiDescription>
  <ApiParameter name="className" type='string' />
</ApiParameters>

### PopoverClose

<ApiParameters>
  <ApiDescription>
    **PopoverClose** extends the Intent UI [**`<DialogClose>`**](/docs/components/overlays/dialog#dialogclose) component.
  </ApiDescription>
  <ApiParameter name="className" type='string' />
  <ApiParameter
    name="intent"
    type='"primary" | "secondary" | "warning" | "danger" | "success" | "outline" | "plain"'
    defaultValue='plain'
  />
  <ApiReference>
    See the [React Aria](https://react-aria.adobe.com/Popover#api) for the full API reference.
  </ApiReference>
</ApiParameters>
