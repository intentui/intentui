---
title: "Select"
description: A customizable dropdown menu that lets users choose a single option from a predefined list, optimized for accessibility and seamless user interaction.
order: 2
published: true
references: ["https://react-aria.adobe.com/Select#api"]
---

<How minW60 toUse="pickers/select/select-example" />

## Installation

Install the component via the CLI in one command.
<PackageCommand command="npx shadcn@latest add @intentui/select"/>

## Composed components
<Composed components={['field', 'dropdown', 'popover']}/>

## Manual installation

Use this approach if you prefer to install and wire up the component yourself instead of using the CLI.
<PackageCommand command="npm i react-aria-components @heroicons/react tailwind-merge"/>

<SourceCode toShow='select'/>

## Anatomy
```
import {
  Select,
  SelectContent,
  SelectItem,
  SelectTrigger,
} from "@/components/ui/select"
```

```
const software = [
  { id: 1, name: "Adobe Photoshop" },
  { id: 2, name: "Sketch" },
  { id: 3, name: "Figma" },
  { id: 4, name: "Adobe XD" },
  { id: 5, name: "InVision" },
]

<Select aria-label="Design software" placeholder="Select a software">
  <SelectTrigger />
  <SelectContent items={software}>
    {(item) => (
      <SelectItem id={item.id} textValue={item.name}>
        {item.name}
      </SelectItem>
    )}
  </SelectContent>
</Select>
```

## Examples

### Section
A select component can display items grouped together in sections.
<How minW60 toUse="pickers/select/select-section-example" />

### Searchable select
Sometimes, you may need to search for an item in a select component. Here's how you can do it.
<How minW60 toUse="pickers/select/select-searchable-example" />

### With icon

<How minW60 toUse="pickers/select/select-with-icon-example" />

### With avatar
<How minW60 toUse="pickers/select/select-with-avatar-example" />

### Multiple select
Enable selecting more than one option by setting `selectionMode` to `multiple`.
<How minW60 toUse="pickers/select/select-multiple-example" />


### Validation
If a user submits the form without selecting an item, an error message will appear.
<How minW60 toUse="pickers/select/select-validation-example" />

### Label
When the `children` option is a string, `Select.Label` will automatically wrap it. However, if the option includes an icon or avatar, you'll need to manually define the wrapper like this:

```
<SelectItem id="discord" textValue="Discord">
  <BrandDiscordIcon />
  <SelectLabel>Discord</SelectLabel>
</SelectItem>
```

### Details description
If you need to provide more information about an option, use the `Description` component to display additional details.
<How minW60 toUse="pickers/select/select-details-description-example" />

### Controlled
You can control the select component by passing the `selectedKey` prop.
<How minW60 toUse="pickers/select/select-controlled-example" />

### Uncontrolled
You can uncontrolled the select component by passing the `defaultSelectedKey` prop.
<How minW60 toUse="pickers/select/select-uncontrolled-example" />


### Disabled
When the select component is disabled, it cannot be interacted with.
<How minW60 toUse="pickers/select/select-disabled-example" />

## Component API

### Select

<ApiParameters>
  <ApiDescription>
    **Select** extends the React Aria Components [**`<Select>`**](https://react-aria.adobe.com/Select#api) component.
  </ApiDescription>
  <ApiParameter name="className" type='string' />
  <ApiParameter name="items" type='Iterable<T, M>' />
</ApiParameters>

### SelectContent

<ApiParameters>
  <ApiDescription>
    **SelectContent** extends the React Aria Components [**`<ListBox>`**](https://react-aria.adobe.com/ListBox#api) component.
  </ApiDescription>
  <ApiParameter name="className" type='string' />
  <ApiParameter name="items" type='Iterable<T>' />
  <ApiParameter name="popover" type='Omit<PopoverProps, "children">' />
</ApiParameters>

### SelectItem

<ApiParameters>
  <ApiDescription>
    **SelectItem** extends the Intent UI [**`<DropdownItem>`**](/docs/components/collections/dropdown#dropdownitem) component.
  </ApiDescription>
  <ApiParameter name="className" type='string' />
  <ApiParameter name="intent" type='"danger" | "warning"' />
</ApiParameters>

### SelectLabel

<ApiParameters>
  <ApiDescription>
    **SelectLabel** extends the Intent UI [**`<DropdownLabel>`**](/docs/components/collections/dropdown#dropdownlabel) component.
  </ApiDescription>
  <ApiParameter name="className" type='string' />
</ApiParameters>

### SelectDescription

<ApiParameters>
  <ApiDescription>
    **SelectDescription** extends the Intent UI [**`<DropdownDescription>`**](/docs/components/collections/dropdown#dropdowndescription) component.
  </ApiDescription>
  <ApiParameter name="className" type='string' />
</ApiParameters>

### SelectSection

<ApiParameters>
  <ApiDescription>
    **SelectSection** extends the Intent UI [**`<DropdownSection>`**](/docs/components/collections/dropdown#dropdownsection) component.
  </ApiDescription>
  <ApiParameter name="className" type='string' />
  <ApiParameter name="title" type='string' />
</ApiParameters>

### SelectSeparator

<ApiParameters>
  <ApiDescription>
    **SelectSeparator** extends the Intent UI [**`<DropdownSeparator>`**](/docs/components/collections/dropdown#dropdownseparator) component.
  </ApiDescription>
  <ApiParameter name="className" type='string' />
</ApiParameters>

### SelectTrigger

<ApiParameters>
  <ApiDescription>
    **SelectTrigger** extends the React Aria Components [**`<Button>`**](https://react-aria.adobe.com/Button#api) component.
  </ApiDescription>
  <ApiParameter name="className" type='string' />
  <ApiParameter name="prefix" type='ReactNode' />
  <ApiReference>
    See the [React Aria](https://react-aria.adobe.com/Select#api) for the full API reference.
  </ApiReference>
</ApiParameters>
