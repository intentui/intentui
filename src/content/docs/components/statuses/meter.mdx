---
title: "Meter"
description: A graphical representation of a measured value within a fixed range, perfect for showing scores, ratings, or resource usage. It provides clear visual feedback.
order: 4
published: true
references: ["https://react-aria.adobe.com/Meter#api"]
---

<How minW60 toUse="statuses/meter/meter-example" />

## Installation

Install the component via the CLI in one command.
<PackageCommand command="npx shadcn@latest add @intentui/meter"/>


## Composed components
<Composed components={['field']}/>

## Manual installation

Use this approach if you prefer to install and wire up the component yourself instead of using the CLI.
<PackageCommand command="npm i react-aria-components cn"/>

<SourceCode toShow='meter'/>

## Anatomy
```
import { Meter } from "@/components/ui/meter"

<Meter label="Storage space" value={32} />
```

## Examples

### Decimal format
By default, the meter will display the value as a percentage. You can change the format of the value by passing a `formatOptions` prop. This example is shown decimal format.

<How minW60 toUse="statuses/meter/meter-decimal-format-example" />

### Currency format
You can also change the currency format of the value by passing a `formatOptions` prop. This example is shown currency format.

<How minW60 toUse="statuses/meter/meter-currency-format-example" />

## Component API

### Meter

<ApiParameters>
  <ApiDescription>
    **Meter** extends the React Aria Components [**`<Meter>`**](https://react-aria.adobe.com/Meter#api) component.
  </ApiDescription>
  <ApiParameter name="className" type='string' />
</ApiParameters>

### MeterHeader

<ApiParameters>
  <ApiDescription>
    **MeterHeader** extends the native **`<div>`** element.
  </ApiDescription>
  <ApiParameter name="className" type='string' />
</ApiParameters>

### MeterTrack

<ApiParameters>
  <ApiDescription>
    **MeterTrack** extends the native **`<div>`** element.
  </ApiDescription>
  <ApiParameter name="className" type='string' />
</ApiParameters>

### MeterValue

<ApiParameters>
  <ApiDescription>
    **MeterValue** extends the native **`<span>`** element.
  </ApiDescription>
  <ApiParameter name="className" type='string' />
  <ApiReference>
    See the [React Aria](https://react-aria.adobe.com/Meter#api) for the full API reference.
  </ApiReference>
</ApiParameters>
