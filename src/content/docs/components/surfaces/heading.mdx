---
title: "Heading"
description: A typographic element that enhances readability and SEO by defining section titles with hierarchical importance, improving content structure and user experience.
order: 4
published: true
---

<How toUse="surfaces/heading/heading-example" />

## Installation

Install the component via the CLI in one command.
<PackageCommand command="npx shadcn@latest add @intentui/heading"/>

## Anatomy
```
import { Heading } from "@/components/ui/heading"
```

```
<>
 <Heading level={1}>The quick brown fox jumps over the lazy dog</Heading>
 <Heading level={2}>The quick brown fox jumps over the lazy dog</Heading>
 <Heading level={3}>The quick brown fox jumps over the lazy dog</Heading>
 <Heading level={4}>The quick brown fox jumps over the lazy dog</Heading>
</>
```

## Manual installation

Use this approach if you prefer to install and wire up the component yourself instead of using the CLI.
<PackageCommand command="npm i cn"/>
<SourceCode toShow='heading'/>

## Examples

### Heading levels
By default, when a heading is used on a [card component](/docs/components/surfaces/card), it will default to level `3`. Titles range from levels `1` to `4`. Each level determines the title type and font size.
<How toUse="surfaces/heading/heading-level-example" />

## Component API
<ApiParameters>
  <ApiDescription>
    **Heading** extends the native **`<h1>`**, **`<h2>`**, **`<h3>`**, and **`<h4>`** elements.
  </ApiDescription>
  <ApiParameter name="className" type='string' />
  <ApiParameter name="level" type='1 | 2 | 3 | 4' defaultValue='1' />
</ApiParameters>
