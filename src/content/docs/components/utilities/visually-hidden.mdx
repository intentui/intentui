---
title: "Visually Hidden"
description: Visually Hidden is a component that hides an element from the screen but still keeps it accessible to screen readers.
order: 1
published: true
references: ["https://react-spectrum.adobe.com/react-aria/VisuallyHidden.html#props"]
---

## Visually Hidden
Visually Hidden is a component that hides an element from the screen but still keeps it accessible to screen readers.
```
import { VisuallyHidden } from "@/components/ui/visually-hidden"

<VisuallyHidden>
  You can't see me, but I'm still accessible to screen readers.
</VisuallyHidden>
```

## Installation
<Installation options={{ isComponent: true }} items={['visually-hidden']}/>

## Manual installation
<Installation options={{ isManual: true }} items={['react-aria-components', 'react-aria']}/>
<SourceCode toShow='visually-hidden'/>
