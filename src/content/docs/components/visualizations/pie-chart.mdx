---
title: "Pie Chart"
description: Pie charts show how categories contribute to a whole. Use the donut variant when you also need to display a summary value in the center.
order: 5
published: true
references: ["https://recharts.org/en-US/api/PieChart"]
---

<How isCenter={false} withNoPadding toUse="visualizations/pie-chart/pie-chart-example" />

## Installation

Install the component via the CLI in one command.

<PackageCommand command="npx shadcn@latest add @intentui/pie-chart" />

## Composed components

<Composed components={['chart']} />

## Manual installation

Install the chart dependencies, then copy the component source into your project.

<PackageCommand command="npm i recharts react-aria-components" />

<SourceCode toShow="pie-chart" />

## Usage

Pass the numeric field to `dataKey` and the category field to `nameKey`. Use the category values as keys in `config` to provide labels and optional colors.

```tsx
<PieChart
  data={data}
  dataKey="amount"
  nameKey="name"
  config={{
    Organic: { label: "Organic" },
    Paid: { label: "Paid" },
  }}
/>
```

## Examples

### Donut

Use the donut variant to leave room for a total or other summary in the center.

<How isCenter={false} withNoPadding toUse="visualizations/pie-chart/pie-chart-donut-example" />

### Custom center label

Pass SVG text as children when the center needs more than the built-in single-line label.

<How isCenter={false} withNoPadding toUse="visualizations/pie-chart/pie-chart-donut-custom-label-example" />

### Interactive

Coordinate the chart with controls to emphasize a selected segment.

<How isCenter={false} withNoPadding toUse="visualizations/pie-chart/pie-chart-interactive-example" />

### Gap

Forward Recharts options through `pieProps` to add spacing between slices.

<How isCenter={false} withNoPadding toUse="visualizations/pie-chart/pie-chart-gap-example" />

### Custom colors

Set colors by category in `config` to keep the visual meaning consistent across charts.

<How isCenter={false} withNoPadding toUse="visualizations/pie-chart/pie-chart-custom-colors-example" />

## Component API

<ApiParameters>
  <ApiDescription>
    **PieChart** extends the Intent UI **`<BaseChart>`** props supported by radial charts.
  </ApiDescription>
  <ApiParameter
    name="chartProps"
    type='Omit<ComponentProps<typeof PieChartPrimitive>, "data" | "stackOffset">'
  />
  <ApiParameter name="children" type='React.ReactNode' />
  <ApiParameter name="className" type='string' />
  <ApiParameter
    name="colors"
    type='readonly (ChartColorKeys | string)[]'
    defaultValue='DEFAULT_COLORS'
  />
  <ApiParameter name="config" type='ChartConfig' />
  <ApiParameter name="containerHeight" type='number' defaultValue='370 desktop / 200 mobile' />
  <ApiParameter name="data" type='Record<string, any>[]' defaultValue='[]' />
  <ApiParameter name="dataKey" type='string' />
  <ApiParameter name="label" type='string' />
  <ApiParameter name="nameKey" type='string' />
  <ApiParameter
    name="pieProps"
    type='Omit<ComponentProps<typeof Pie>, "data" | "dataKey" | "name">'
  />
  <ApiParameter name="showLabel" type='boolean' />
  <ApiParameter
    name="tooltip"
    type='TooltipContentType<ValueType, NameType> | boolean'
    defaultValue='true'
  />
  <ApiParameter name="tooltipProps" type='BaseChartProps["tooltipProps"]' />
  <ApiParameter
    name="valueFormatter"
    type='(value: number) => string'
    defaultValue='(value) => value.toString()'
  />
  <ApiParameter name="variant" type='"pie" | "donut"' defaultValue='pie' />
  <ApiReference>
    See the [Recharts PieChart API reference](https://recharts.org/en-US/api/PieChart) for primitive-specific options.
  </ApiReference>
</ApiParameters>
