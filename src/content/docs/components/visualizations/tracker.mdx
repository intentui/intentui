---
title: "Tracker"
description: Tracker components are used to monitor and visualize data changes over time, providing insights into trends, patterns, and performance metrics.
order: 5
published: true
---

<How  toUse="visualizations/tracker/tracker-example" />

## Installation

Install the component via the CLI in one command.
<PackageCommand command="npx shadcn@latest add @intentui/tracker"/>

## Manual installation

Use this approach if you prefer to install and wire up the component yourself instead of using the CLI.
<PackageCommand command="npm i react-aria-components tailwind-merge"/>

<SourceCode toShow='tracker' />

## Examples

### Without tooltip
Sometimes you might want to display the tracker without tooltips for a cleaner look.
<How toUse="visualizations/tracker/tracker-without-tooltip-example" />

## Component API
<ApiParameters>
  <ApiDescription>
    **Tracker** extends the native **`<div>`** element.
  </ApiDescription>
  <ApiParameter name="className" type='string' />
  <ApiParameter name="data" type='TrackerBlockProps[]' defaultValue='[]' />
  <ApiParameter name="defaultBackgroundColor" type='string' />
</ApiParameters>
