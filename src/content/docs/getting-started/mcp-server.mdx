---
title: 'MCP Server'
description: Browse, search, and install registry components via MCP. This tool provides an efficient way to manage and access design system components for streamlined development.
order: 4
published: true
---

## Registry

To use the Intent UI registry, add the following entry to your `components.json`:

```json
{
  "registries": {
    "@intentui": "https://intentui.com/r/{name}"
  }
}
```

## Initialize

Choose a client and run one of the following commands:

<McpTabs />

That's it – you can now browse, search, and install registry components via MCP! To learn more, check out the [Shadcn MCP documentation](https://ui.shadcn.com/docs/mcp?utm_source=intentui.com&utm_medium=referral&utm_campaign=docs&utm_content=%2Fdocs%2Fmcp).
