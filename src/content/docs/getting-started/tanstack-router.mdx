---
title: "TanStack Router"
description: "Set up Intent in your TanStack Router project with Tailwind CSS using a simple and efficient installation process. Get started quickly with minimal setup."
---

## Installation
Start a new project with the following command:
```bash
npx create-tsrouter-app@latest app --template file-router --tailwind
```

Next, initialize Intent in your project:
```bash
npx shadcn@latest init @intentui/theme-default
```

## Adding Components
Add Intent components to your project with the following command:
```bash
npx shadcn@latest add @intentui/text-field @intentui/button @intentui/menu
```

## Starter Kit
Get started quickly with our TanStack Router Starter Kit, which includes Intent preconfigured. Clone the repository [here](https://github.com/intentui/tanstack-router) and start building your project immediately.
